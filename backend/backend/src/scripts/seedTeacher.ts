/**
 * NOTE: This file is a duplicate located in backend/backend/ by mistake.
 * The canonical version lives at: backend/src/scripts/seedTeacher.ts
 *
 * All 3 IDE errors stem from this file being in the wrong directory
 * (no node_modules, no tsconfig, can't resolve models or process globals).
 *
 * Please run the seed from the correct location:
 *   cd family-hub/backend
 *   npm run seed
 *
 * This file intentionally left as a no-op redirect to avoid confusion.
 */

console.log(
  "⚠️  Wrong location! Run: cd family-hub/backend && npm run seed"
);
