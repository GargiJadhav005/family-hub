{
  "name": "family-hub-tests",
  "version": "1.0.0",
  "description": "Family Hub — end-to-end and unit tests",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "@vitest/coverage-v8": "^1.6.0",
    "supertest": "^7.0.0",
    "@types/supertest": "^6.0.2",
    "dotenv": "^16.4.5"
  }
}
